# 🛡️ LiteLLM Backup & Recovery Guide

Комплексное руководство по системе резервного копирования и восстановления данных.

> **✅ СТАТУС: Система полностью настроена и работает!**
> - ✅ Автоматические бэкапы каждую ночь в 3:00
> - ✅ Удаленная синхронизация на 65.21.202.252 (yan@hetz)
> - ✅ Telegram уведомления настроены
> - ✅ External Docker volumes (защита от случайного удаления)
> - 📅 Дата настройки: 28 октября 2025

## 📋 Содержание

- [Обзор системы](#обзор-системы)
- [Быстрый старт](#быстрый-старт)
- [Структура бэкапов](#структура-бэкапов)
- [Настройка Telegram уведомлений](#настройка-telegram-уведомлений)
- [Настройка удаленного сервера](#настройка-удаленного-сервера)
- [Автоматизация через cron](#автоматизация-через-cron)
- [Восстановление данных](#восстановление-данных)
- [Защита от потери данных](#защита-от-потери-данных)
- [Частые вопросы](#частые-вопросы)

---

## 🎯 Обзор системы

Система реализует стратегию **3-2-1 backup**:
- **3** копии данных (оригинал + 2 бэкапа)
- **2** разных носителя (локальный диск + удаленный сервер)
- **1** копия offsite (на удаленном сервере)

### Что бэкапится:
- ✅ **PostgreSQL** - база данных с логами, командами, бюджетами (~1.5GB)
- ✅ **Grafana** - дашборды и настройки
- ✅ **Конфигурации** - docker-compose.yml, config.yaml, .env, prometheus.yml
- ❌ **Prometheus** - метрики (можно пересобрать из PostgreSQL)
- ❌ **Redis** - кэш (временные данные)

### Политика ротации:
- **Daily** (ежедневные): последние 7 дней
- **Weekly** (еженедельные): последние 4 недели (каждое воскресенье)
- **Monthly** (ежемесячные): последние 12 месяцев (1-е число месяца)

---

## 🚀 Быстрый старт

### 1. Создание бэкапа вручную

```bash
# Базовый бэкап
./scripts/backup.sh

# Результат:
# ✅ Создан бэкап в backups/daily/YYYY-MM-DD_HHMMSS/
# ✅ Обновлен симлинк backups/latest
# ✅ Старые бэкапы автоматически удалены (если > 7 дней)
```

### 2. Восстановление из бэкапа

```bash
# Интерактивный выбор
./scripts/restore.sh

# Или прямое указание бэкапа
./scripts/restore.sh backups/daily/2025-10-28_002820
```

### 3. Синхронизация с удаленным сервером

```bash
# После настройки SSH (см. ниже)
./scripts/sync-remote.sh
```

---

## 📁 Структура бэкапов

```
litellm/
├── backups/
│   ├── daily/                    # Ежедневные бэкапы (7 дней)
│   │   ├── 2025-10-28_002820/
│   │   │   ├── postgresql_litellm.sql       # Дамп БД
│   │   │   ├── grafana/                     # Дашборды Grafana
│   │   │   ├── configs/                     # Конфиги
│   │   │   │   ├── docker-compose.yml.backup
│   │   │   │   ├── config.yaml.backup
│   │   │   │   ├── .env.backup
│   │   │   │   └── prometheus.yml.backup
│   │   │   ├── BACKUP_INVENTORY.txt         # Список файлов
│   │   │   └── RESTORE.sh                   # Скрипт восстановления
│   │   └── ...
│   ├── weekly/                   # Еженедельные (4 недели)
│   │   ├── 2025-W43/
│   │   └── ...
│   ├── monthly/                  # Ежемесячные (12 месяцев)
│   │   ├── 2025-10/
│   │   └── ...
│   ├── latest -> daily/...       # Симлинк на последний
│   └── .backup_status            # JSON статус последнего бэкапа
├── scripts/
│   ├── backup.sh                 # Основной скрипт бэкапа
│   ├── restore.sh                # Восстановление
│   ├── sync-remote.sh            # Синхронизация
│   ├── telegram-notify.sh        # Уведомления
│   └── docker-safe.sh            # Защита Docker
└── ...
```

### Пример BACKUP_INVENTORY.txt:

```
===============================================
LiteLLM Backup Inventory
===============================================
Backup Date: 2025-10-28 00:28:20
Backup Type: daily
Backup Location: /home/yan/litellm/backups/daily/2025-10-28_002820
===============================================

Contents:
postgresql_litellm.sql           1.5G
configs/docker-compose.yml.backup    5.1K
configs/config.yaml.backup          1.7K
grafana/provisioning/...            ...

Total Size: 1.5G
```

---

## 📱 Настройка Telegram уведомлений

### Шаг 1: Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "LiteLLM Backup Bot")
4. Введите username (например: "litellm_backup_bot")
5. Скопируйте **токен** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получение Chat ID

1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. Бот ответит вашим **Chat ID** (число, например: `123456789`)

### Шаг 3: Настройка .env

Откройте файл `.env` и заполните:

```bash
# Backup Configuration
TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
TELEGRAM_CHAT_ID="123456789"
```

### Шаг 4: Проверка

```bash
./scripts/telegram-notify.sh test

# Результат:
# ✅ Telegram test successful!
# Вы получите тестовое сообщение в Telegram
```

### Примеры уведомлений:

```bash
# Успешный бэкап
./scripts/telegram-notify.sh success /path/to/backup 1.5GB 120

# Ошибка
./scripts/telegram-notify.sh failure "Database connection failed"

# Предупреждение
./scripts/telegram-notify.sh warning "Disk space low"

# Информация
./scripts/telegram-notify.sh info "Backup process started"

# Кастомное сообщение
./scripts/telegram-notify.sh custom success "Title" "Body text"
```

---

## 🌐 Настройка удаленного сервера

### Требования:
- Удаленный сервер доступный по SSH
- `rsync` установлен (обычно есть по умолчанию)
- SSH key authentication настроен

### Шаг 1: Настройка SSH ключа

```bash
# Если у вас нет SSH ключа, создайте его:
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопируйте ключ на удаленный сервер:
ssh-copy-id -p 22 root@65.21.202.252

# Проверьте подключение:
ssh root@65.21.202.252 "echo 'SSH работает!'"
```

### Шаг 2: Настройка .env (опционально)

Если хотите изменить параметры по умолчанию:

```bash
# Remote Backup Server
BACKUP_REMOTE_HOST="65.21.202.252"      # IP или hostname
BACKUP_REMOTE_USER="root"               # SSH пользователь
BACKUP_REMOTE_PATH="/var/backups/litellm"  # Путь на сервере
BACKUP_REMOTE_PORT="22"                 # SSH порт
```

### Шаг 3: Первая синхронизация

```bash
./scripts/sync-remote.sh

# Результат:
# ✅ SSH connection successful
# ✅ Remote directory created: /var/backups/litellm
# ✅ Syncing daily backups...
# ✅ Syncing weekly backups...
# ✅ Syncing monthly backups...
# ✅ Sync completed successfully
```

### Проверка удаленных бэкапов:

```bash
ssh root@65.21.202.252 "du -sh /var/backups/litellm && find /var/backups/litellm -type d -mindepth 2 -maxdepth 2"

# Результат:
# 1.5G  /var/backups/litellm
# /var/backups/litellm/daily/2025-10-28_002820
```

---

## ⏰ Автоматизация через cron

### Рекомендуемое расписание:

```bash
# Открыть crontab
crontab -e

# Добавить следующие строки:
# ┌───────────── минута (0-59)
# │ ┌───────────── час (0-23)
# │ │ ┌───────────── день месяца (1-31)
# │ │ │ ┌───────────── месяц (1-12)
# │ │ │ │ ┌───────────── день недели (0-7, 0=воскресенье)
# │ │ │ │ │

# Ежедневный бэкап в 3:00 ночи + синхронизация
0 3 * * * cd /home/yan/litellm && ./scripts/backup.sh >> /home/yan/litellm/backup.log 2>&1 && ./scripts/sync-remote.sh >> /home/yan/litellm/sync.log 2>&1
```

### Альтернативные варианты:

```bash
# Каждые 6 часов (для критичных систем)
0 */6 * * * cd /home/yan/litellm && ./scripts/backup.sh >> /home/yan/litellm/backup.log 2>&1

# Только в рабочие дни в 3:00
0 3 * * 1-5 cd /home/yan/litellm && ./scripts/backup.sh >> /home/yan/litellm/backup.log 2>&1

# Синхронизация отдельно в 4:00 (после бэкапа)
0 4 * * * cd /home/yan/litellm && ./scripts/sync-remote.sh >> /home/yan/litellm/sync.log 2>&1
```

### Проверка логов:

```bash
# Логи бэкапов
tail -f backup.log

# Логи синхронизации
tail -f sync.log
```

---

## 🔄 Восстановление данных

### Сценарий 1: Интерактивное восстановление

```bash
./scripts/restore.sh

# Процесс:
# 1. Показывает список доступных бэкапов (daily/weekly/monthly)
# 2. Выбираете тип бэкапа (daily/weekly/monthly)
# 3. Выбираете конкретный бэкап из списка
# 4. Показывает информацию о бэкапе
# 5. Запрашивает подтверждение
# 6. Останавливает сервисы
# 7. Восстанавливает базу данных
# 8. Восстанавливает Grafana
# 9. Восстанавливает конфиги (с подтверждением для .env)
# 10. Запускает сервисы
```

### Сценарий 2: Прямое восстановление

```bash
# Восстановление из конкретного бэкапа
./scripts/restore.sh backups/daily/2025-10-28_002820

# Восстановление последнего бэкапа
./scripts/restore.sh backups/latest
```

### Сценарий 3: Восстановление с удаленного сервера

```bash
# 1. Скачать бэкап с удаленного сервера
rsync -avz --progress \
  root@65.21.202.252:/var/backups/litellm/daily/2025-10-28_002820/ \
  /home/yan/litellm/backups/daily/2025-10-28_002820/

# 2. Восстановить локально
./scripts/restore.sh backups/daily/2025-10-28_002820
```

### Сценарий 4: Быстрое восстановление из бэкапа (RESTORE.sh в каждом бэкапе)

```bash
# Каждый бэкап содержит собственный RESTORE.sh
cd backups/daily/2025-10-28_002820
./RESTORE.sh

# Этот скрипт автоматически:
# ✅ Останавливает сервисы
# ✅ Восстанавливает базу данных
# ✅ Восстанавливает конфиги
# ✅ Восстанавливает Grafana
# ✅ Запускает сервисы
```

### Проверка после восстановления:

```bash
# 1. Проверить статус сервисов
docker compose ps

# 2. Проверить базу данных
PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm \
  -c "SELECT COUNT(*) FROM \"LiteLLM_SpendLogs\";"

# 3. Проверить LiteLLM
curl http://localhost:4000/health/liveliness

# 4. Проверить Grafana
curl http://localhost:3098/api/health
```

---

## 🛡️ Защита от потери данных

### 1. External Docker Volumes

**Проблема:** Команда `docker compose down -v` удаляет все volumes, включая базу данных.

**Решение:** Volumes PostgreSQL и Grafana настроены как `external`, поэтому они **НЕ УДАЛЯЮТСЯ** при `docker compose down -v`.

```yaml
# docker-compose.yml
volumes:
  postgres_data:
    name: litellm_postgres_data_external
    external: true  # ← Защита от удаления
  grafana_data:
    name: litellm_grafana_data_external
    external: true  # ← Защита от удаления
```

### 2. Docker Safe Wrapper

Используйте `docker-safe.sh` вместо прямых docker команд:

```bash
# ✅ Безопасно - используйте wrapper
./scripts/docker-safe.sh down
./scripts/docker-safe.sh restart
./scripts/docker-safe.sh up -d

# ❌ Опасно - прямые команды
docker compose down -v  # БЕЗ ЗАЩИТЫ!
```

**Что делает docker-safe.sh:**
- Обнаруживает опасные команды (`down -v`, `rm -f`, `volume prune`)
- Показывает предупреждение
- Требует двойное подтверждение ("DELETE VOLUMES" + "YES")
- Автоматически создает бэкап перед выполнением
- Блокирует выполнение при отмене

### 3. Автоматические бэкапы перед операциями

```bash
# Создать бэкап перед обновлением
./scripts/backup.sh
git pull
docker compose up -d --build

# Создать бэкап перед миграцией БД
./scripts/backup.sh
docker compose exec litellm python migrate_db.py
```

### 4. Алиасы для безопасной работы

Добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
# Безопасные алиасы для Docker
alias dcup='cd /home/yan/litellm && ./scripts/docker-safe.sh up -d'
alias dcdown='cd /home/yan/litellm && ./scripts/docker-safe.sh down'
alias dcrestart='cd /home/yan/litellm && ./scripts/docker-safe.sh restart'
alias dclogs='cd /home/yan/litellm && ./scripts/docker-safe.sh logs -f'
alias dcps='cd /home/yan/litellm && ./scripts/docker-safe.sh ps'

# Бэкапы
alias backup='cd /home/yan/litellm && ./scripts/backup.sh'
alias restore='cd /home/yan/litellm && ./scripts/restore.sh'
alias sync-backup='cd /home/yan/litellm && ./scripts/sync-remote.sh'
```

После добавления:
```bash
source ~/.bashrc  # или source ~/.zshrc
```

---

## ❓ Частые вопросы

### Q: Сколько места занимают бэкапы?

**A:** Зависит от размера базы данных:
- **1 бэкап** ≈ 1.5GB (база данных + конфиги)
- **7 daily** = ~10.5GB
- **4 weekly** = ~6GB
- **12 monthly** = ~18GB
- **Итого:** ~35GB для полного цикла

Hard links экономят место для одинаковых файлов между бэкапами.

### Q: Как восстановить только базу данных?

**A:** Используйте прямой psql:
```bash
PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy litellm \
  < backups/daily/2025-10-28_002820/postgresql_litellm.sql
```

### Q: Как восстановить только Grafana?

**A:**
```bash
backup_dir="backups/daily/2025-10-28_002820"
cp -r "${backup_dir}/grafana/provisioning" grafana/
docker compose restart grafana
```

### Q: Можно ли бэкапить на внешний диск?

**A:** Да, измените симлинк:
```bash
# Смонтируйте диск в /mnt/external_backup
ln -sf /mnt/external_backup /home/yan/litellm/backups
```

### Q: Как проверить целостность бэкапа?

**A:** Скрипт restore.sh автоматически проверяет:
- Существование директории бэкапа
- Наличие SQL файла
- Читаемость SQL файла
- Минимальный размер файла (>1KB)

Вручную:
```bash
# Проверить SQL файл
head -n 10 backups/daily/2025-10-28_002820/postgresql_litellm.sql

# Проверить размер
du -h backups/daily/2025-10-28_002820/postgresql_litellm.sql

# Попробовать восстановить в тестовую БД
createdb test_restore
psql test_restore < backups/daily/2025-10-28_002820/postgresql_litellm.sql
dropdb test_restore
```

### Q: Как удалить старые бэкапы вручную?

**A:** Скрипт backup.sh автоматически удаляет старые, но вручную:
```bash
# Удалить бэкапы старше 30 дней
find backups/daily -type d -mtime +30 -exec rm -rf {} \;

# Удалить конкретный бэкап
rm -rf backups/daily/2025-10-01_030000
```

### Q: Telegram уведомления не приходят

**A:** Проверьте:
1. Токен и Chat ID правильные в .env
2. Бот запущен (отправьте /start боту в Telegram)
3. Тест работает: `./scripts/telegram-notify.sh test`
4. Проверьте интернет соединение
5. Убедитесь что в .env нет лишних пробелов

### Q: Ошибка "SSH connection failed"

**A:**
```bash
# 1. Проверьте SSH ключ
ssh -v root@65.21.202.252

# 2. Если нужен пароль, настройте SSH ключ
ssh-copy-id root@65.21.202.252

# 3. Проверьте права на ключ
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub

# 4. Проверьте фаервол
ping 65.21.202.252
telnet 65.21.202.252 22
```

### Q: База данных восстановилась, но сервисы не работают

**A:**
```bash
# 1. Проверьте логи
docker compose logs litellm
docker compose logs db

# 2. Перезапустите сервисы
docker compose restart litellm litellm-metrics-exporter

# 3. Проверьте подключение к БД
PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm \
  -c "SELECT 1;"
```

### Q: Как мигрировать бэкапы на новый сервер?

**A:**
```bash
# 1. На старом сервере: создать финальный бэкап
./scripts/backup.sh

# 2. Скопировать весь проект на новый сервер
rsync -avz /home/yan/litellm/ new-server:/home/yan/litellm/

# 3. На новом сервере: восстановить
cd /home/yan/litellm
docker compose up -d
./scripts/restore.sh backups/latest
```

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи:**
   ```bash
   tail -f backup.log
   docker compose logs -f litellm
   docker compose logs -f db
   ```

2. **Проверьте статус:**
   ```bash
   cat backups/.backup_status
   docker compose ps
   ```

3. **Запустите тесты:**
   ```bash
   ./scripts/telegram-notify.sh test
   ./scripts/sync-remote.sh --help
   ```

4. **Создайте issue на GitHub** с логами и описанием проблемы

---

## 📚 Связанная документация

- [DOCKER_COMMANDS.md](DOCKER_COMMANDS.md) - Безопасные Docker команды
- [GIT_WORKFLOW.md](GIT_WORKFLOW.md) - Git workflow для форка
- [README.md](README.md) - Основная документация проекта

---

**✨ Ваши данные под надежной защитой!**
