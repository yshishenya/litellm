# DNS Update Instructions

## Инструкции по обновлению DNS записей для миграции LiteLLM

**Целевой IP:** 65.21.202.252
**Домены:** litellm.pro-4.ru, dash.pro-4.ru

---

## ⚠️ ВАЖНО: Когда обновлять DNS

**Обновляйте DNS ТОЛЬКО когда:**
- ✅ Все тесты на новом сервере прошли успешно
- ✅ Скрипт `./scripts/post-migration-verify.sh` показал 0 критичных ошибок
- ✅ Вы готовы к возможному 5-10 минутному простою
- ✅ Вы уведомили всех пользователей
- ✅ У вас есть план отката (см. ROLLBACK_PLAN.md)

**НЕ обновляйте DNS если:**
- ❌ Есть критичные ошибки на новом сервере
- ❌ Тесты не пройдены
- ❌ PostgreSQL не содержит все данные
- ❌ API не отвечает на новом сервере

---

## Шаг 1: Подготовка к переключению

### 1.1. Финальная проверка нового сервера

```bash
# Подключиться к новому серверу
ssh yan@65.21.202.252
cd /home/yan/litellm

# Запустить полную верификацию
./scripts/post-migration-verify.sh

# Убедиться, что все проверки успешны
```

### 1.2. Записать текущие IP адреса

```bash
# Проверить текущие DNS записи ПЕРЕД изменением
nslookup litellm.pro-4.ru
nslookup dash.pro-4.ru

# Записать старые IP для возможного отката
# litellm.pro-4.ru → [ЗАПИШИТЕ СТАРЫЙ IP]
# dash.pro-4.ru → [ЗАПИШИТЕ СТАРЫЙ IP]
```

### 1.3. Проверить TTL

```bash
# Проверить текущий TTL
dig litellm.pro-4.ru | grep -A1 "ANSWER SECTION"
dig dash.pro-4.ru | grep -A1 "ANSWER SECTION"

# Если TTL > 300 секунд, рекомендуется:
# 1. Сначала уменьшить TTL до 300
# 2. Подождать старый TTL
# 3. Затем менять IP
```

### 1.4. Уведомить пользователей

```bash
# Отправить уведомление о предстоящем переключении
./scripts/telegram-notify.sh "🔄 Начинается переключение DNS. Возможен кратковременный простой 5-10 минут."
```

### 1.5. Записать время начала

```bash
# Записать точное время начала простоя
date > /tmp/migration_start_time.txt
echo "Начало простоя: $(cat /tmp/migration_start_time.txt)"
```

---

## Шаг 2: Обновление DNS записей

### Вариант A: Через панель управления DNS (рекомендуется)

Точные шаги зависят от вашего DNS провайдера. Общая процедура:

#### Для litellm.pro-4.ru:

1. **Войдите в панель управления DNS**
   - Откройте веб-интерфейс вашего DNS провайдера
   - Найдите зону для домена `pro-4.ru`

2. **Найдите A-запись для litellm**
   - Ищите запись типа: `litellm.pro-4.ru` или просто `litellm`
   - Тип: `A` (IPv4 Address)

3. **Измените IP адрес**
   - Старое значение: [текущий IP старого сервера]
   - **Новое значение: `65.21.202.252`**

4. **Установите TTL (Time To Live)**
   - Рекомендуется: `300` секунд (5 минут)
   - Это ускорит распространение изменений

5. **Сохраните изменения**

#### Для dash.pro-4.ru:

1. **Найдите A-запись для dash**
   - Ищите запись: `dash.pro-4.ru` или просто `dash`
   - Тип: `A` (IPv4 Address)

2. **Измените IP адрес**
   - Старое значение: [текущий IP старого сервера]
   - **Новое значение: `65.21.202.252`**

3. **Установите TTL**
   - Рекомендуется: `300` секунд

4. **Сохраните изменения**

---

### Вариант B: Через API/CLI (если поддерживается)

#### Cloudflare (пример):

```bash
# Установите переменные
CLOUDFLARE_API_TOKEN="your_api_token"
ZONE_ID="your_zone_id"
NEW_IP="65.21.202.252"

# Обновить litellm.pro-4.ru
curl -X PUT "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/dns_records/RECORD_ID" \
  -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
  -H "Content-Type: application/json" \
  --data '{
    "type": "A",
    "name": "litellm.pro-4.ru",
    "content": "'$NEW_IP'",
    "ttl": 300,
    "proxied": false
  }'

# Обновить dash.pro-4.ru
curl -X PUT "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/dns_records/RECORD_ID" \
  -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
  -H "Content-Type: application/json" \
  --data '{
    "type": "A",
    "name": "dash.pro-4.ru",
    "content": "'$NEW_IP'",
    "ttl": 300,
    "proxied": false
  }'
```

#### DigitalOcean (пример):

```bash
# Установите переменные
DO_API_TOKEN="your_do_token"
DOMAIN="pro-4.ru"
NEW_IP="65.21.202.252"

# Обновить litellm
curl -X PUT "https://api.digitalocean.com/v2/domains/$DOMAIN/records/RECORD_ID" \
  -H "Authorization: Bearer $DO_API_TOKEN" \
  -H "Content-Type: application/json" \
  --data '{
    "type": "A",
    "name": "litellm",
    "data": "'$NEW_IP'",
    "ttl": 300
  }'

# Обновить dash
curl -X PUT "https://api.digitalocean.com/v2/domains/$DOMAIN/records/RECORD_ID" \
  -H "Authorization: Bearer $DO_API_TOKEN" \
  -H "Content-Type: application/json" \
  --data '{
    "type": "A",
    "name": "dash",
    "data": "'$NEW_IP'",
    "ttl": 300
  }'
```

---

### Вариант C: Другие провайдеры

**Популярные DNS провайдеры:**

1. **Reg.ru**
   - Личный кабинет → Домены → pro-4.ru → DNS серверы и записи зоны
   - Найти A-записи для litellm и dash
   - Изменить IP на 65.21.202.252
   - TTL: 300 секунд

2. **Cloudflare**
   - Dashboard → pro-4.ru → DNS
   - Найти A-записи
   - Edit → Изменить IPv4 address на 65.21.202.252
   - TTL: Auto или 5 minutes

3. **Route53 (AWS)**
   - Hosted zones → pro-4.ru
   - Найти Record sets для litellm и dash
   - Edit → Value: 65.21.202.252
   - TTL: 300

4. **Google Cloud DNS**
   - Cloud DNS → pro-4.ru
   - Найти A records
   - Edit → IPv4 Address: 65.21.202.252
   - TTL: 300

---

## Шаг 3: Проверка распространения DNS

### 3.1. Локальная проверка (может показывать старый IP из кеша)

```bash
# Проверить DNS резолвинг
nslookup litellm.pro-4.ru
nslookup dash.pro-4.ru

# Если видите старый IP, не паникуйте - это кеш
# Очистите локальный DNS кеш:

# macOS
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder

# Linux (systemd-resolved)
sudo systemd-resolve --flush-caches

# Linux (nscd)
sudo /etc/init.d/nscd restart

# Windows
ipconfig /flushdns
```

### 3.2. Проверка с авторитетных DNS серверов

```bash
# Google DNS
nslookup litellm.pro-4.ru 8.8.8.8
nslookup dash.pro-4.ru 8.8.8.8

# Ожидаемый результат:
# Address: 65.21.202.252

# Cloudflare DNS
nslookup litellm.pro-4.ru 1.1.1.1
nslookup dash.pro-4.ru 1.1.1.1

# OpenDNS
nslookup litellm.pro-4.ru 208.67.222.222
nslookup dash.pro-4.ru 208.67.222.222
```

### 3.3. Проверка с помощью dig (более детально)

```bash
# Проверка litellm.pro-4.ru
dig @8.8.8.8 litellm.pro-4.ru +short
# Должно вернуть: 65.21.202.252

dig litellm.pro-4.ru +noall +answer
# Проверить TTL и IP

# Проверка dash.pro-4.ru
dig @8.8.8.8 dash.pro-4.ru +short
# Должно вернуть: 65.21.202.252
```

### 3.4. Проверка с разных географических точек

**Онлайн сервисы для проверки DNS:**

1. **WhatsmyDNS.net** (рекомендуется)
   - Откройте: https://www.whatsmydns.net/
   - Введите: `litellm.pro-4.ru`
   - Тип: `A`
   - Проверьте, что большинство серверов показывают `65.21.202.252`
   - Повторите для `dash.pro-4.ru`

2. **DNSChecker.org**
   - Откройте: https://dnschecker.org/
   - Введите домен
   - Проверьте глобальное распространение

3. **Google DNS Check**
   - Откройте: https://dns.google/
   - Введите домен
   - Проверьте результат

### 3.5. Мониторинг распространения

```bash
# Автоматическая проверка каждые 10 секунд
watch -n 10 'echo "=== litellm.pro-4.ru ===" && nslookup litellm.pro-4.ru 8.8.8.8 | grep Address && echo "=== dash.pro-4.ru ===" && nslookup dash.pro-4.ru 8.8.8.8 | grep Address'

# Или с помощью скрипта:
while true; do
  IP1=$(dig @8.8.8.8 litellm.pro-4.ru +short)
  IP2=$(dig @8.8.8.8 dash.pro-4.ru +short)

  echo "[$(date '+%H:%M:%S')] litellm: $IP1 | dash: $IP2"

  if [ "$IP1" = "65.21.202.252" ] && [ "$IP2" = "65.21.202.252" ]; then
    echo "✓ DNS propagated!"
    break
  fi

  sleep 10
done
```

---

## Шаг 4: Проверка доступности через новые DNS

### 4.1. Проверка LiteLLM API

```bash
# Проверка через IP (должно работать всегда)
curl http://65.21.202.252:4000/health/liveliness

# Проверка через домен (после распространения DNS)
curl http://litellm.pro-4.ru:4000/health/liveliness

# Если используется HTTPS
curl https://litellm.pro-4.ru/health/liveliness

# Проверка models endpoint
curl http://litellm.pro-4.ru:4000/v1/models
```

### 4.2. Проверка Grafana

```bash
# Проверка через IP
curl http://65.21.202.252:3098/api/health

# Проверка через домен
curl http://dash.pro-4.ru:3098/api/health

# Если используется HTTPS
curl https://dash.pro-4.ru/api/health
```

### 4.3. Проверка в браузере

Откройте в браузере:

1. **LiteLLM (если есть UI):**
   - http://litellm.pro-4.ru:4000
   - или https://litellm.pro-4.ru

2. **Grafana:**
   - http://dash.pro-4.ru:3098
   - или https://dash.pro-4.ru
   - Логин: admin
   - Пароль: admin123

3. **Проверьте:**
   - [ ] Дашборды загружаются
   - [ ] Данные отображаются
   - [ ] Графики обновляются
   - [ ] Нет ошибок в консоли браузера

---

## Шаг 5: Финализация

### 5.1. Остановка старого сервера (НЕ СРАЗУ!)

**ВАЖНО:** НЕ останавливайте старый сервер минимум 48 часов!

```bash
# На старом сервере (только через 48 часов)
# ssh [старый_сервер]
# cd /home/yan/litellm
# docker compose stop
```

### 5.2. Записать время окончания

```bash
# Записать время окончания простоя
date > /tmp/migration_end_time.txt
echo "Окончание простоя: $(cat /tmp/migration_end_time.txt)"

# Вычислить длительность простоя
START=$(cat /tmp/migration_start_time.txt)
END=$(cat /tmp/migration_end_time.txt)
echo "Начало: $START"
echo "Конец: $END"
```

### 5.3. Уведомить пользователей

```bash
# Отправить уведомление о завершении
./scripts/telegram-notify.sh "✅ Миграция завершена! DNS обновлен, сервисы работают на новом сервере."
```

### 5.4. Запустить финальную верификацию

```bash
# На новом сервере
ssh yan@65.21.202.252
cd /home/yan/litellm
./scripts/post-migration-verify.sh
```

---

## Таймлайн распространения DNS

**Типичные времена распространения:**

| Локация | Время | Комментарий |
|---------|-------|-------------|
| Авторитетный DNS | 0-30 сек | Ваш DNS провайдер |
| Google DNS (8.8.8.8) | 1-5 мин | Обычно очень быстро |
| Cloudflare (1.1.1.1) | 1-5 мин | Обычно очень быстро |
| Локальные ISP | 5-15 мин | Зависит от TTL |
| Все серверы мира | 15-60 мин | Полное распространение |

**С TTL=300 (5 минут):**
- Минимальное время: 5 минут
- Типичное время: 10-15 минут
- Максимальное время: 30-60 минут

---

## Проблемы и их решение

### Проблема: DNS не обновляется

**Симптомы:**
- `nslookup` показывает старый IP
- Сайты недоступны через домен

**Решения:**

1. **Очистите локальный DNS кеш** (см. Шаг 3.1)

2. **Проверьте с авторитетных серверов:**
   ```bash
   nslookup litellm.pro-4.ru 8.8.8.8
   ```

3. **Подождите TTL время:**
   - Если старый TTL был большим (например, 3600), нужно ждать этот период

4. **Проверьте, что изменения сохранены:**
   - Войдите обратно в панель DNS
   - Убедитесь, что IP = 65.21.202.252

---

### Проблема: DNS обновился, но сайт недоступен

**Симптомы:**
- `nslookup` показывает правильный IP (65.21.202.252)
- Но `curl` не работает

**Решения:**

1. **Проверьте, что сервисы запущены:**
   ```bash
   ssh yan@65.21.202.252 "docker ps"
   ```

2. **Проверьте порты:**
   ```bash
   ssh yan@65.21.202.252 "netstat -tulpn | grep -E '4000|3098'"
   ```

3. **Проверьте firewall:**
   ```bash
   ssh yan@65.21.202.252 "sudo ufw status"
   # Должны быть открыты порты 4000, 3098
   ```

4. **Проверьте логи:**
   ```bash
   ssh yan@65.21.202.252 "cd /home/yan/litellm && docker compose logs --tail=50"
   ```

---

### Проблема: Часть пользователей видит старый сервер

**Симптомы:**
- Некоторые пользователи успешно подключаются
- Другие видят старый сервер или ошибки

**Причина:**
- DNS кеширование у разных ISP

**Решение:**
- **Подождите:** Это нормально, DNS распространяется постепенно
- **Не паникуйте:** Процесс может занять до 30-60 минут
- **Сообщите пользователям:** Попросите очистить DNS кеш (см. Шаг 3.1)

---

### Проблема: SSL сертификат не работает

**Симптомы:**
- HTTPS выдает ошибку сертификата
- HTTP работает

**Решения:**

1. **Проверьте, что SSL настроен на новом сервере:**
   ```bash
   ssh yan@65.21.202.252 "curl -k https://localhost:4000/health/liveliness"
   ```

2. **Перевыпустите SSL сертификат для нового IP:**
   - Если используете Let's Encrypt:
     ```bash
     sudo certbot renew --force-renewal
     ```

3. **Обновите конфигурацию nginx/apache** (если используется)

---

## Откат DNS (если нужно)

**Если возникли проблемы:**

### Быстрый откат

1. **Верните старые IP в DNS:**
   - litellm.pro-4.ru → [старый IP]
   - dash.pro-4.ru → [старый IP]

2. **Установите минимальный TTL:**
   - TTL: 60 или 300 секунд

3. **Дождитесь распространения:**
   - 5-15 минут

4. **Запустите старый сервер:**
   ```bash
   ssh [старый_сервер]
   cd /home/yan/litellm
   docker compose up -d
   ```

**Детальнее см.:** [ROLLBACK_PLAN.md](./ROLLBACK_PLAN.md)

---

## Чек-лист обновления DNS

### Перед обновлением:
- [ ] Новый сервер полностью протестирован
- [ ] Все проверки пройдены успешно
- [ ] Пользователи уведомлены
- [ ] Записаны старые IP адреса для отката
- [ ] TTL установлен на минимум (300 сек)
- [ ] План отката готов

### Обновление DNS:
- [ ] Обновлена A-запись для litellm.pro-4.ru → 65.21.202.252
- [ ] Обновлена A-запись для dash.pro-4.ru → 65.21.202.252
- [ ] TTL установлен на 300 секунд
- [ ] Изменения сохранены

### Проверка распространения:
- [ ] Проверено с Google DNS (8.8.8.8)
- [ ] Проверено с Cloudflare DNS (1.1.1.1)
- [ ] Проверено на whatsmydns.net
- [ ] Большинство серверов показывают новый IP

### Проверка доступности:
- [ ] LiteLLM API отвечает через домен
- [ ] Grafana доступна через домен
- [ ] Дашборды загружаются
- [ ] Нет ошибок в логах

### Финализация:
- [ ] Записано время простоя
- [ ] Пользователи уведомлены о завершении
- [ ] Запущена финальная верификация
- [ ] Старый сервер оставлен на 48 часов

---

## Полезные ссылки

**Онлайн инструменты для проверки DNS:**
- https://www.whatsmydns.net/ - Глобальная проверка DNS
- https://dnschecker.org/ - Альтернативная проверка
- https://dns.google/ - Google DNS lookup
- https://mxtoolbox.com/DNSLookup.aspx - Детальная DNS информация

**DNS провайдеры:**
- Cloudflare: https://dash.cloudflare.com/
- DigitalOcean: https://cloud.digitalocean.com/networking/domains
- Route53: https://console.aws.amazon.com/route53/
- Reg.ru: https://www.reg.ru/user/domain_list

---

**Документ обновлен:** $(date)
**Целевой IP:** 65.21.202.252
**Домены:** litellm.pro-4.ru, dash.pro-4.ru
