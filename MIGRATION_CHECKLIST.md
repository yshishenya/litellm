# LiteLLM Migration Checklist

## Детальный чеклист миграции на новый сервер (Blue-Green Deployment)

**Целевой сервер:** 65.21.202.252
**Пользователь:** yan
**Ожидаемое время:** ~1 час (простой 5-10 минут)
**Метод:** Blue-Green Deployment

---

## Фаза 0: Подготовка (до миграции)

### ☐ 0.1. Документация и коммуникация

- [ ] Создать план миграции и согласовать с командой
- [ ] Определить временное окно миграции
- [ ] Уведомить всех пользователей о предстоящей миграции
- [ ] Подготовить контакты для экстренной связи
- [ ] Сохранить текущие доступы и пароли в безопасном месте

### ☐ 0.2. Проверка текущей системы

- [ ] Проверить статус всех контейнеров: `docker ps`
- [ ] Проверить работоспособность API: `curl http://localhost:4000/health/liveliness`
- [ ] Проверить количество записей в БД:
  ```bash
  PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm -c "SELECT COUNT(*) FROM \"LiteLLM_SpendLogs\""
  ```
- [ ] Сохранить текущее количество записей для сверки после миграции
- [ ] Проверить размер данных: `du -sh /var/lib/docker/volumes/litellm_*`

### ☐ 0.3. Проверка нового сервера

- [ ] Проверить доступность нового сервера: `ping 65.21.202.252`
- [ ] Проверить SSH доступ: `ssh yan@65.21.202.252`
- [ ] Проверить версию Docker: `ssh yan@65.21.202.252 "docker --version"`
- [ ] Проверить версию Docker Compose: `ssh yan@65.21.202.252 "docker compose version"`
- [ ] Проверить свободное место: `ssh yan@65.21.202.252 "df -h"`
- [ ] Убедиться, что на новом сервере минимум 20GB свободного места

### ☐ 0.4. Настройка SSH

- [ ] Настроить SSH ключи для беспарольного доступа:
  ```bash
  ssh-copy-id yan@65.21.202.252
  ```
- [ ] Проверить беспарольный доступ: `ssh yan@65.21.202.252 "echo OK"`

---

## Фаза 1: Предварительные проверки (5-10 минут)

### ☐ 1.1. Запуск скрипта проверки

- [ ] Запустить предварительную проверку:
  ```bash
  cd /home/yan/litellm
  ./scripts/pre-migration-check.sh
  ```
- [ ] Убедиться, что все проверки пройдены успешно
- [ ] Устранить все критические ошибки, если есть
- [ ] Записать результаты проверки

### ☐ 1.2. Проверка бэкапов

- [ ] Проверить наличие последнего бэкапа: `ls -lh backups/latest`
- [ ] Проверить целостность бэкапа:
  ```bash
  test -f backups/latest/postgresql_litellm.sql && echo "OK" || echo "FAIL"
  test -f backups/latest/.env && echo "OK" || echo "FAIL"
  test -f backups/latest/docker-compose.yml && echo "OK" || echo "FAIL"
  ```
- [ ] Проверить возраст бэкапа (не старше 24 часов):
  ```bash
  find backups/latest -name "postgresql_litellm.sql" -mtime -1
  ```

---

## Фаза 2: Создание финального бэкапа (10-15 минут)

### ☐ 2.1. Финальный бэкап

- [ ] Запустить финальный бэкап:
  ```bash
  ./scripts/backup.sh
  ```
- [ ] Дождаться завершения бэкапа
- [ ] Проверить логи бэкапа: `tail -50 backup.log`
- [ ] Убедиться, что бэкап успешно создан
- [ ] Записать путь к бэкапу: `readlink -f backups/latest`

### ☐ 2.2. Синхронизация на удаленный сервер

- [ ] Синхронизировать бэкап на новый сервер:
  ```bash
  ./scripts/sync-remote.sh
  ```
- [ ] Проверить, что бэкап появился на новом сервере:
  ```bash
  ssh yan@65.21.202.252 "ls -lh /home/yan/backups/litellm/"
  ```

### ☐ 2.3. Снимок состояния

- [ ] Сделать снимок текущей конфигурации:
  ```bash
  docker compose config > docker-compose.snapshot.yml
  docker ps > containers.snapshot.txt
  docker volume ls > volumes.snapshot.txt
  ```
- [ ] Сохранить количество записей в БД для сверки:
  ```bash
  PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm \
    -c "SELECT COUNT(*) FROM \"LiteLLM_SpendLogs\"" > pre_migration_count.txt
  ```

---

## Фаза 3: Автоматическая миграция (20-30 минут)

### ☐ 3.1. Запуск скрипта миграции

- [ ] Запустить автоматический скрипт миграции:
  ```bash
  ./scripts/migrate-to-new.sh
  ```
- [ ] Следить за выводом скрипта
- [ ] Подтверждать действия по запросу скрипта
- [ ] Записывать время начала каждого этапа

### ☐ 3.2. Мониторинг процесса

Скрипт автоматически выполнит:
- [x] Копирование конфигурационных файлов
- [x] Копирование бэкапов
- [x] Создание Docker volumes на новом сервере
- [x] Восстановление PostgreSQL
- [x] Запуск всех сервисов
- [x] Базовое тестирование

### ☐ 3.3. Проверка успешности

- [ ] Убедиться, что скрипт завершился успешно (exit code 0)
- [ ] Проверить логи миграции: `cat migration_*.log`
- [ ] Убедиться, что получено уведомление в Telegram

---

## Фаза 4: Тестирование на новом сервере (15-20 минут)

### ☐ 4.1. Подключение к новому серверу

- [ ] Подключиться к новому серверу:
  ```bash
  ssh yan@65.21.202.252
  cd /home/yan/litellm
  ```

### ☐ 4.2. Проверка контейнеров

- [ ] Проверить статус контейнеров: `docker ps`
- [ ] Убедиться, что запущено 6 контейнеров:
  - [ ] litellm_proxy
  - [ ] litellm_db
  - [ ] redis
  - [ ] prometheus
  - [ ] grafana
  - [ ] litellm-metrics-exporter
- [ ] Проверить логи на ошибки: `docker compose logs --tail=50`

### ☐ 4.3. Проверка сервисов

- [ ] **LiteLLM Proxy:**
  ```bash
  curl http://localhost:4000/health/liveliness
  # Ожидается: {"status":"I'm alive!"}

  curl http://localhost:4000/health/readiness
  # Ожидается: 200 OK

  curl http://localhost:4000/v1/models
  # Ожидается: список моделей
  ```

- [ ] **PostgreSQL:**
  ```bash
  PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm \
    -c "SELECT COUNT(*) FROM \"LiteLLM_SpendLogs\""
  # Сверить с pre_migration_count.txt

  PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm \
    -c "SELECT COUNT(*) FROM \"LiteLLM_UserTable\""

  PGPASSWORD=dbpassword9090 psql -h localhost -p 5433 -U llmproxy -d litellm \
    -c "SELECT COUNT(*) FROM \"LiteLLM_TeamTable\""
  ```

- [ ] **Redis:**
  ```bash
  docker exec $(docker ps -qf "name=redis") redis-cli ping
  # Ожидается: PONG
  ```

- [ ] **Prometheus:**
  ```bash
  curl http://localhost:9092/-/healthy
  # Ожидается: Prometheus is Healthy

  curl http://localhost:9092/api/v1/targets | jq
  # Проверить, что targets up
  ```

- [ ] **Grafana:**
  ```bash
  curl http://localhost:3098/api/health
  # Ожидается: {"database":"ok"}

  curl -u admin:admin123 http://localhost:3098/api/datasources | jq
  # Проверить наличие Prometheus и PostgreSQL

  curl -u admin:admin123 http://localhost:3098/api/search?type=dash-db | jq
  # Проверить наличие дашбордов
  ```

- [ ] **Metrics Exporter:**
  ```bash
  curl http://localhost:9090/metrics | grep litellm_team_spend
  # Ожидается: метрики с данными
  ```

### ☐ 4.4. Функциональное тестирование

- [ ] Попробовать сделать тестовый запрос к API (если есть тестовый ключ)
- [ ] Открыть Grafana в браузере: `http://65.21.202.252:3098`
- [ ] Войти в Grafana (admin/admin123)
- [ ] Проверить, что дашборды отображают данные
- [ ] Проверить, что графики строятся

### ☐ 4.5. Запуск полной верификации

- [ ] Запустить скрипт верификации:
  ```bash
  ./scripts/post-migration-verify.sh
  ```
- [ ] Убедиться, что все проверки пройдены
- [ ] Устранить предупреждения, если есть

---

## Фаза 5: Переключение DNS (ПРОСТОЙ 5-10 минут)

### ☐ 5.1. Подготовка к переключению

- [ ] Убедиться, что все тесты на новом сервере прошли успешно
- [ ] Записать текущее время начала простоя
- [ ] Уведомить пользователей о начале переключения

### ☐ 5.2. Перевод старого сервера в read-only (опционально)

Если нужен read-only режим:
- [ ] На старом сервере остановить сервис записи или перевести в read-only
- [ ] Создать финальный инкрементальный бэкап
- [ ] Применить изменения на новом сервере

### ☐ 5.3. Остановка старого сервера

- [ ] На старом сервере остановить контейнеры:
  ```bash
  cd /home/yan/litellm
  docker compose stop
  ```
- [ ] Убедиться, что все контейнеры остановлены: `docker ps`

### ☐ 5.4. Обновление DNS записей

**Для домена litellm.pro-4.ru:**
- [ ] Войти в панель управления DNS
- [ ] Найти A-запись для litellm.pro-4.ru
- [ ] Изменить IP адрес на: **65.21.202.252**
- [ ] Установить TTL: 300 (5 минут) для быстрого распространения
- [ ] Сохранить изменения
- [ ] Записать время обновления

**Для домена dash.pro-4.ru:**
- [ ] Найти A-запись для dash.pro-4.ru
- [ ] Изменить IP адрес на: **65.21.202.252**
- [ ] Установить TTL: 300 (5 минут)
- [ ] Сохранить изменения
- [ ] Записать время обновления

### ☐ 5.5. Проверка распространения DNS

- [ ] Проверить DNS с локальной машины:
  ```bash
  nslookup litellm.pro-4.ru
  nslookup dash.pro-4.ru
  ```

- [ ] Проверить DNS с внешних серверов:
  ```bash
  # Google DNS
  nslookup litellm.pro-4.ru 8.8.8.8

  # Cloudflare DNS
  nslookup litellm.pro-4.ru 1.1.1.1
  ```

- [ ] Использовать онлайн сервисы:
  - https://www.whatsmydns.net/
  - https://dnschecker.org/

- [ ] Дождаться распространения на большинстве серверов (обычно 5-15 минут)

### ☐ 5.6. Проверка доступности через домены

- [ ] Проверить LiteLLM через домен:
  ```bash
  curl http://litellm.pro-4.ru:4000/health/liveliness
  ```

- [ ] Проверить Grafana через домен:
  ```bash
  curl http://dash.pro-4.ru:3098/api/health
  ```

- [ ] Открыть Grafana в браузере: http://dash.pro-4.ru:3098
- [ ] Убедиться, что дашборды загружаются

### ☐ 5.7. Конец простоя

- [ ] Записать время окончания простоя
- [ ] Вычислить общее время простоя
- [ ] Уведомить пользователей о завершении миграции

---

## Фаза 6: Пост-миграционная проверка (10-15 минут)

### ☐ 6.1. Мониторинг первых минут

- [ ] Проверить логи всех контейнеров на ошибки:
  ```bash
  docker compose logs --tail=100 -f
  ```
- [ ] Мониторить использование ресурсов:
  ```bash
  docker stats
  ```
- [ ] Проверить доступность всех endpoints

### ☐ 6.2. Проверка функциональности

- [ ] Попробовать сделать реальный запрос к API
- [ ] Проверить, что метрики обновляются в Grafana
- [ ] Проверить, что логи пишутся в PostgreSQL
- [ ] Проверить, что Redis кеширование работает

### ☐ 6.3. Запуск полной верификации

- [ ] Запустить финальную верификацию:
  ```bash
  ./scripts/post-migration-verify.sh
  ```
- [ ] Убедиться, что все проверки успешны

---

## Фаза 7: Настройка автоматизации (5-10 минут)

### ☐ 7.1. Настройка автоматических бэкапов

- [ ] На новом сервере настроить cron задачи:
  ```bash
  cd /home/yan/litellm
  ./scripts/setup-cron.sh
  ```
- [ ] Выбрать расписание (рекомендуется: ежедневно в 3:00)
- [ ] Проверить, что cron задача создана:
  ```bash
  crontab -l | grep litellm
  ```

### ☐ 7.2. Обновление remote backup target (если нужно)

- [ ] Если используете другой сервер для бэкапов, обновите конфигурацию в `scripts/sync-remote.sh`
- [ ] Если новый сервер = backup сервер, настройте синхронизацию на третий сервер или облако

### ☐ 7.3. Проверка Telegram уведомлений

- [ ] Проверить, что Telegram уведомления работают:
  ```bash
  ./scripts/telegram-notify.sh "Тест после миграции"
  ```
- [ ] Убедиться, что сообщение получено

---

## Фаза 8: Документация и очистка (10-15 минут)

### ☐ 8.1. Документирование миграции

- [ ] Записать фактическое время простоя
- [ ] Записать все проблемы и их решения
- [ ] Обновить документацию с новыми IP/hostname
- [ ] Сохранить логи миграции в безопасное место

### ☐ 8.2. Обновление документации проекта

- [ ] Обновить README с новыми адресами
- [ ] Обновить инструкции по подключению
- [ ] Обновить контакты и доступы

### ☐ 8.3. Старый сервер (НЕ УДАЛЯТЬ 48 часов!)

- [ ] Оставить старый сервер запущенным (контейнеры остановлены)
- [ ] Пометить в календаре дату возможного удаления (текущая дата + 48 часов)
- [ ] Задокументировать процедуру отката (см. ROLLBACK_PLAN.md)

---

## Фаза 9: Мониторинг (24-48 часов)

### ☐ 9.1. Первые 24 часа

- [ ] Каждый час проверять:
  - [ ] Доступность API
  - [ ] Grafana дашборды
  - [ ] Логи на ошибки
  - [ ] Использование ресурсов

- [ ] Мониторить метрики производительности:
  - [ ] Время отклика API
  - [ ] Использование CPU/RAM
  - [ ] Использование диска
  - [ ] Количество запросов

### ☐ 9.2. После 24 часов

- [ ] Запустить полную верификацию:
  ```bash
  ./scripts/post-migration-verify.sh
  ```
- [ ] Проверить, что бэкапы создаются автоматически
- [ ] Проверить логи бэкапов: `tail -100 backup.log`
- [ ] Убедиться, что нет критических проблем

### ☐ 9.3. После 48 часов

- [ ] Запустить финальную верификацию
- [ ] Проанализировать все метрики за 48 часов
- [ ] Убедиться, что система стабильна
- [ ] Принять решение об окончательном выводе старого сервера

---

## Фаза 10: Финализация (после 48 часов стабильной работы)

### ☐ 10.1. Решение о старом сервере

- [ ] Убедиться, что новый сервер работает стабильно 48+ часов
- [ ] Создать финальный бэкап старого сервера (на всякий случай)
- [ ] Решить: удалить или оставить старый сервер как холодный резерв

### ☐ 10.2. Очистка старого сервера (опционально)

Если решено удалить:
- [ ] Создать финальный архив всех данных
- [ ] Сохранить архив в безопасное место
- [ ] Остановить и удалить все контейнеры:
  ```bash
  docker compose down -v
  ```
- [ ] Удалить volumes:
  ```bash
  docker volume rm litellm_postgres_data_external
  docker volume rm litellm_grafana_data_external
  ```
- [ ] Очистить директорию: `rm -rf /home/yan/litellm`

### ☐ 10.3. Финальная документация

- [ ] Обновить runbook с опытом миграции
- [ ] Записать lessons learned
- [ ] Обновить процедуры disaster recovery
- [ ] Провести post-mortem встречу (если есть команда)

### ☐ 10.4. Уведомления

- [ ] Уведомить всех заинтересованных об успешной миграции
- [ ] Обновить контакты и документацию
- [ ] Отправить финальный отчет

---

## Чек-лист отката (Rollback)

**В случае критических проблем:**

### ☐ Экстренный откат

- [ ] Переключить DNS обратно на старый сервер
- [ ] Запустить контейнеры на старом сервере:
  ```bash
  docker compose up -d
  ```
- [ ] Проверить работоспособность
- [ ] Уведомить пользователей
- [ ] Провести анализ проблем

**Детали в файле:** [ROLLBACK_PLAN.md](./ROLLBACK_PLAN.md)

---

## Контакты и ресурсы

**Telegram уведомления:**
- Bot: 7965448155:AAGSiq8Ngvw-Z2sKtWbwabaSL-HnvRPpkNg
- Chat ID: 234583347

**Серверы:**
- Старый сервер: [текущий hostname/IP]
- Новый сервер: 65.21.202.252
- Backup сервер: 65.21.202.252 (тот же)

**Важные файлы:**
- Логи миграции: `/home/yan/litellm/migration_*.log`
- Бэкапы: `/home/yan/litellm/backups/`
- Конфигурация: `/home/yan/litellm/.env`

**Полезные команды:**
```bash
# Статус сервисов
docker compose ps

# Логи
docker compose logs -f

# Проверка здоровья
./scripts/post-migration-verify.sh

# Бэкап
./scripts/backup.sh

# Откат
# См. ROLLBACK_PLAN.md
```

---

## Итоговая проверка

После завершения всех фаз:

- [ ] ✅ Все 6 контейнеров работают
- [ ] ✅ API отвечает на запросы
- [ ] ✅ PostgreSQL содержит все данные
- [ ] ✅ Grafana отображает дашборды
- [ ] ✅ Prometheus собирает метрики
- [ ] ✅ DNS указывает на новый сервер
- [ ] ✅ Автоматические бэкапы настроены
- [ ] ✅ Система стабильна 48+ часов
- [ ] ✅ Документация обновлена
- [ ] ✅ Пользователи уведомлены

**МИГРАЦИЯ ЗАВЕРШЕНА! 🎉**
